{
  "name": "app-lib",
  "version": "0.0.1",
  "description": "",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf ./build",
    "build::pre": "npm run clean && ts-node src/build.ts",
    "build": "npm run clean && npm run build::pre && npx tsc"
  },
  "keywords": [],
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.0.6",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "toml": "^3.0.0"
  }
}
